spring:
  datasource:
    url: jdbc:tc:postgresql:12:///spring-app
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
server:
  tomcat:
    threads:
      max: 100
  port: 9999
